import { EnumlistPipeTsPipe } from './enumlist.pipe.ts.pipe';

describe('EnumlistPipeTsPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumlistPipeTsPipe();
    expect(pipe).toBeTruthy();
  });
});

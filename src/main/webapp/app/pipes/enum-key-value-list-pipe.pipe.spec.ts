import { EnumKeyValueListPipePipe } from './enum-key-value-list-pipe.pipe';

describe('EnumKeyValueListPipePipe', () => {
  it('create an instance', () => {
    const pipe = new EnumKeyValueListPipePipe();
    expect(pipe).toBeTruthy();
  });
});
